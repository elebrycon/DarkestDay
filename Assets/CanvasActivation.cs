using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CanvasActivation : MonoBehaviour
{
    public uiActivate canvas;
    // Start is called before the first frame update
    void Start()
    {
        canvas.ActivationOfCanvas();
    }

   
}
